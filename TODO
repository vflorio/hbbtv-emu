- Modalità iFrame per avere un contesto con la runtime già inizializzata

Player 
    - Gestire correttamente TagGroup Playable (come ad esempio in caso di errore, o in caso di waiting non dovremmo essere Playable)
    
    - Sistema avanzato di Source Switching quando abbiamo più provider per lo stesso media ( o due sorgenti per lo stesso media), quando un provider fallisce, automaticamente proviamo il prossimo provider

- Logger Env: console controllabile da Extension State (quindi UI)

- Possibilità di importare files locali nello storage dell'estensione

- Import MPEG-TS + parsing del carousel DSM-CC

- Chrome V2: creare estensione + Inject Strategy


