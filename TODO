- AV Object:
    Utilizzo: 
            const object = document.createElement("object");
            object.style.cssText = "width:1280px;height:720px;position:absolute;z-index:1000;top:0;left:0;";
            object.type = "application/dash+xml";
            object.data = "https://d1x2qd46lfkzk0.cloudfront.net/daitest/multiaac2/index.mpd";
            document.body.appendChild(object);
            object.play(1);

- Logger Env: console controllabile da Extension State (quindi UI)

- Possibilità di importare files locali nello storage dell'estensione

- Import MPEG-TS + parsing del carousel DSM-CC

- Chrome V2: creare estensione + Inject Strategy


